//Función extractor
//Tiene como argumento una lista de elementos, imprime uno a uno sus elementos
function extractor(lista) {
  lista.forEach((n) => console.log(n));
}
liste = ['amigo', 'amiga', 'conocido', 'companero'];
extractor(liste);
