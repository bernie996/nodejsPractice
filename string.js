var listaDeNombres = ['Saul', 'Carlos', 'Bernie', 'Renier', 'Adrian', 'Jesus'];
if (listaDeNombres[0] === 'Bernie') {
  console.log(`${listaDeNombres[0]} is the first one like always`);
} else {
  console.log(
    `${listaDeNombres[0]} is the first one for the first and last time`
  );
}
